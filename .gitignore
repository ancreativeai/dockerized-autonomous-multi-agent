# secrets — never commit (use .env.example as the template)
.env

# multi-GB GGUF model cache
hermes/

# runtime state & logs (regenerated by the stack)
audit-logs/
openclaw-data/
workspaces/done/
workspaces/worker-*/
workspaces/openclaw/reports/

# OpenClaw source clone target (only mounted for live dev)
openclaw/

# personal knowledge base — keep private
obsidian-vault/

# old debug dumps
all-files-export.txt
dir-tree.txt

__pycache__/
*.pyc
